version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./src/:/app/src/
      - ./public/:/app/public
      - ./.env/:/app/.env
    environment:
      - NODE_ENV=development
    env_file:
      - ./.env