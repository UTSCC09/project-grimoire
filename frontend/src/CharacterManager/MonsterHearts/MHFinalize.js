import React from "react";
import PropTypes from 'prop-types'
import { Grid, Typography } from "@mui/material";

function MHFinalize(props){
    return(
        <Grid>
            <Typography>Finalize your sheet</Typography>
        </Grid>
    )
}

export default MHFinalize